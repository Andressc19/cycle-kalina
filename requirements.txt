# Dependencias del proyecto ciclo Kalina KSC-11 (Python 3.13.x)
# Instalación recomendada:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# El camino real de la mezcla NH3-H2O es el motor iapws portado
# (src/properties/_nh3h2o_engine.py + _kalina_flash.py, ver CONTEXT.md).
iapws==1.5.5
numpy==2.5.3
scipy==1.18.1
pandas==3.0.6
openpyxl==3.1.5
matplotlib==3.11.2
streamlit==1.64.0
pytest==9.1.1
pytest-cov==7.1.0

# TeqpAdapter (src/properties/teqp_adapter.py): alternativa de rendimiento a
# AmmoniaWaterAdapter, ~20x más rápida (ver TASK_CONTEXT 2026-09-18-validar-
# teqp-nh3h2o). Usa teqp.AmmoniaWaterTillnerRoth (mismo modelo Tillner-Roth &
# Friend 1998 que IAPWS G4-01, dominio público NIST, sin licencia REFPROP) +
# CoolProp para el gas ideal de los puros. AmmoniaWaterAdapter sigue siendo
# el camino real por defecto (validado exhaustivamente contra IAPWS G4-01);
# TeqpAdapter no lo está — es una alternativa experimental de rendimiento.
teqp==0.23.2
CoolProp==8.0.0

# Opcional (solo referencia/futuro): pyfluids NO cubre la mezcla NH3-H2O en
# este equipo aun con CoolProp instalado (falta el par binario NH3-H2O en los
# backends libres de CoolProp, ver CONTEXT.md). PyfluidsAdapter se conserva
# como referencia; no se instala por defecto.
# pyfluids>=4.0.0